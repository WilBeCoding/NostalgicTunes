extends ../layout

block content
    h1 Playlist shoe page
    h2 For all your shoe needs
    each playlist in userPlaylists
      p
        a(href='/users/playlist/' + playlist._id)=playlist.playlist
    //- p #{user.email}
    //- p #{userPlaylists}